# ChronForge recursive roadmap — qstack-governed development

Program: [#1](https://github.com/tommy-ca/chronforge/issues/1)  
Quant-development orchestration: [`tommy-ca/qstack`](https://github.com/tommy-ca/qstack)  
Orchestration binding: [#10](https://github.com/tommy-ca/chronforge/issues/10)

Qstack owns the quant-development composition and verification contracts for ChronForge while delegating generic software-engineering workflow mechanics to its upstream pstack substrate. ChronForge owns code, tests, issues, PRs, and runtime evidence.

## Top-level graph

```text
O0 qstack binding
  -> D0 baseline characterization
      -> D1 engine contracts
          -> D2 hftbacktest integration
              -> D3 native strategy runtime
                  +-> D4 optional FFI
                  +-> D5 optional live core
                        -> D6 optional governance/recovery
```

Mandatory MVP: **O0 -> D0 -> D1 -> D2 -> D3**.

## Recursive issue graph

```text
#10 O0 qstack orchestration binding
  |
  v
#2 D0 characterize external kernel
  #11 pin/evidence ---------+
  #12 kernel/seam inventory +--> #14 D0.J baseline receipt
  #13 executable goldens ---+
  |
  v
#3 D1 engine contracts
  #15 IDs/units/identities -+
  #16 EventPhase/EventKey --+
  #17 execution state ------+--> #19 D1.J formal contract receipt
  #18 account/result -------+
  |
  v
#4 D2 hftbacktest integration
  #20 dependency/market ----+
  #21 command bridge -------+
  #22 execution projection -+--> #24 D2.J Runtime integration receipt
  #23 100-run determinism --+
  |
  v
#5 D3 native runtime
  #25 RuntimeEvent/source --+
  #26 Strategy/Context -----+
  #27 runtime loop ---------+--> #29 D3.J verified software artifact
  #28 artifact/qorch -------+
  |
  +--> #6 D4 optional FFI
  +--> #7 D5 optional live
          +--> #8 D6 optional governance/recovery
```

## Readiness

| Unit | Status | Readiness rule |
|---|---|---|
| O0 #10 | **READY** | project orchestration/profile binding |
| D0 #2 | **READY for characterization** | implementation claims require O0 binding; D0 completes only through #14 |
| D1 #3 | BLOCKED | requires accepted #14 |
| D2 #4 | BLOCKED | requires accepted #19 |
| D3 #5 | BLOCKED | requires accepted #24 |
| D4 #6 | OPTIONAL | requires accepted #29 + explicit FFI selection |
| D5 #7 | OPTIONAL | requires accepted #29 + explicit live selection |
| D6 #8 | OPTIONAL | requires D5 + explicit governance/recovery ADR |

## Qstack profile map

| Phase | Qstack overlay |
|---|---|
| D0 | QD-15 + deterministic-runtime-audit |
| D1 | QD-06 + formal-state-model + replay-parity |
| D2 | QD-05 + QD-06 + deterministic-runtime-audit + replay-parity |
| D3 | QD-16 + strategy-runtime-contract + replay-parity |
| D4 | QD-16 with FFI capability selected |
| D5 | QD-01 + QD-10 + bounded-hot-paths |
| D6 | QD-08/QD-09/QD-13 only when selected |

## Join rule

Leaf issues may run in parallel where their explicit dependencies permit it, but **only the join issue advances the phase**:

```text
D0 -> #14
D1 -> #19
D2 -> #24
D3 -> #29
```

Every join receipt must be revision-specific, name its qstack semantic/profile revision, list acceptance IDs, include verification commands/results, and distinguish Static/Metadata evidence from Runtime evidence.

## Research handoff

D3.J emits `SoftwareArtifactRef`, `RunReceipt`, and `DeterminismReceipt`. Quant research then moves to qstack `qorch`:

```text
ChronForge verified artifact
  -> qorch ResearchProgram/Study artifact edge
  -> qorch program-ready
  -> agent/SearchRunner research
  -> ChronForge RunReceipt
  -> qorch validation/candidate/promotion evidence
```

Qorch does not mirror ChronForge branches, worktrees, PR state, or development worker assignments.

## Evidence honesty

- READY means graph readiness, not Runtime PASS.
- Static/Metadata/CI PASS does not imply PAPER/LIVE PASS.
- Timeout != PASS.
- Optional issue existence does not imply capability selection.
