[package]
name = "hbt-runtime"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "ChronForge strategy runtime (scaffold Soft≠green)"
repository.workspace = true

[features]
default = []
# Optional foreign ABI Soft≠green — empty until D4 selects FFI.
abi = []

[dependencies]
hbt-engine = { path = "../hbt-engine" }
