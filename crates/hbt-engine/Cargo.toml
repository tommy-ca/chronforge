[package]
name = "hbt-engine"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "ChronForge deterministic domain engine (scaffold Soft≠green)"
repository.workspace = true

[dependencies]
# Soft≠green: do NOT add hard dep on remote hftbacktest yet.
# Dependency law: engine MAY depend on hftbacktest later (after D0 pin);
# never reverse (hftbacktest must never depend on hbt-engine).
# See crate README and docs/architecture/boundaries.md.
